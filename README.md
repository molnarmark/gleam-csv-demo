# 🌠 Pure Gleam CSV Parser Demo

This demo implementation shows how parsing CSV files would work in pure [Gleam](http://gleam.run).


## Test it out

Clone this repo, `cd` into the directory and run

```bash
rebar3 escriptize
```

then

```bash
./_build/default/bin/csv_demo
```

**Note that this is Work In Progress**